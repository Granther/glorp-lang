glunc mlorp() {
    try {
        var x = splink()
    } 
    ohshit {
        print "Error in try"
    }
}

glunc splink() {
    print "splink"
    wert "x should NOT be true"
}
// All functions can evaluate to a wert if one is werted
// If it does, then the var a\associated with oshit is assigned the wert val
// Simply, it allows use to raise and pass simple user defined errors